#pragma once
#include "pch.h"
#include <string>
#include "degree_measure.h"
using namespace std;

bool is_Index(string str);
bool is_Number(string str);
bool is_Number_Of_Menu(string str);
bool is_Multiplier(string str);
bool is_angle(string str);
bool is_angle_(string str);
